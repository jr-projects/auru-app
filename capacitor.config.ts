import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.auru.edu.app',
  appName: 'auru-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
